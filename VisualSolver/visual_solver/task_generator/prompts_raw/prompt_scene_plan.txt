You are an expert in educational content design, instructional design, and math education. Please design a structured explanation document to solve the given math problem with diagrams and text.

**Problem Overview:**

Topic: {topic}
Description: {description}

**Structure Requirements:**

The output should be a **Markdown-friendly outline** that interleaves:
- `<TEXT_k>` blocks: written explanation
- `<SCENE_k>` blocks: diagram specifications to render

Please generate the outline in the following format:

```xml
<SCENE_OUTLINE>
    <TEXT_1>
    [Text content: first restate the full problem statement verbatim, and briefly state the goal]
    </TEXT_1>

    <SCENE_1>
    Scene Title: [Title]
    Scene Purpose: [Learning objective, connection to previous content]
    Scene Description: [What diagram to draw; list key objects: points/lines/labels/aux lines/highlights]
    Scene Layout: [Spatial layout concept, consider safe area and spacing]
    </SCENE_1>

    <TEXT_2>
    [Text explanation that refers to the diagram above; step-by-step reasoning]
    </TEXT_2>

    <SCENE_2>
    Scene Title: [Title]
    Scene Purpose: [Learning objective, connection to previous content]
    Scene Description: [Second diagram if needed; include auxiliary lines/dashed lines/area shading]
    Scene Layout: [Spatial layout concept, consider safe area and spacing]
    </SCENE_2>

    <TEXT_3>
    [Continue explanation...]
    </TEXT_3>

    <SCENE_3>
    ...
    </SCENE_3>

    ...

    <TEXT_N>
    [Final answer + verification + short recap]
    </TEXT_N>
</SCENE_OUTLINE>
```

**Content Flow (must follow):**
1) TEXT: restate problem
2) SCENE: initial problem diagram
3) TEXT: explanation
4) SCENE: auxiliary-line diagram (if needed)
5) TEXT: explanation
...
Final TEXT must include: final answer, verification, recap.

**Scene count:** 1-7 scenes.
**Hard requirement:** Every `<SCENE_k>` must have at least one adjacent `<TEXT_k>` block so the Markdown reads naturally.

**Requirements (must-follow):**
1. `<TEXT_1>` must include the *full problem statement verbatim* and briefly state the goal.
2. `<SCENE_1>` must describe a clear **problem diagram** that visualizes the setup (e.g., coordinate plane + points/polygons for geometry).
3. Each `<SCENE_k>` description must be specific about what elements to include (points, lines, labels, colors, annotations).
4. Each `<TEXT_k>` should be written as if explaining to a student: clear, step-by-step reasoning.
5. Use diagrams/visualizations to support reasoning; avoid text-only explanations.
6. Structure constraints:
   - Start with problem statement + initial diagram
   - Include intermediate steps with supporting diagrams (only when needed)
   - End with final answer + verification; optional short recap.
7. Diagram specifications:
   - Include coordinate values for geometry problems
   - Specify colors for highlighting (e.g., "highlight triangle ADC in red")
   - Specify auxiliary lines/dashed lines where needed
   - Label all key points clearly

Note: This is a high-level outline. Each `<SCENE_k>` will later be rendered as a static image using Manim, and all `<TEXT_k>` blocks will be compiled into a Markdown document with embedded diagrams.